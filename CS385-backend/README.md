# CS385-Jansport-Project

Records Management System (RMS) built on AWS.

## Setup

```bash
npm install
```

## Deploy

See AWS Lambda deployment docs.

## API Endpoints

- `POST /upload` - Upload a document
- `GET /files` - List user's documents
- `GET /files/{fileId}` - Get document details
